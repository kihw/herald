// Configuration runtime pour l'API
window.APP_CONFIG = {
  API_BASE: window.location.protocol + '//' + window.location.host,
  API_PATH: '/api'
};

console.log('App configured for:', window.APP_CONFIG.API_BASE);
